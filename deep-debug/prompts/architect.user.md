Symptom: $symptom

All hypotheses explored:
$hypotheses_json

Three fix attempts failed across distinct hypotheses. Diagnose at the pattern level: wrong shared-state model, violated invariant, or wrong abstraction? Name the architectural question and propose alternatives.
